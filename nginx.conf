worker_processes 1;
error_log /dev/stderr warn;
pid /tmp/nginx.pid;

events { worker_connections 1024; }

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    sendfile on;
    access_log /dev/stdout;

    # Apply request-body limit at server scope so every location inherits it.
    # Whisper hard-caps at 25 MB, but the client compresses before upload, so
    # this should never be hit in practice — large value is just safety margin.
    client_max_body_size 500m;

    server {
        listen RAILWAY_PORT;
        root /usr/share/nginx/html;
        index index.html;

        # Belt-and-suspenders: also set at server scope
        client_max_body_size 500m;

        # Proxy all /api/* to the Node tools server on port 3007
        location /api/ {
            proxy_pass http://127.0.0.1:3007/api/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            client_max_body_size 500m;
            proxy_read_timeout 300s;
            proxy_connect_timeout 60s;
            proxy_send_timeout 300s;
        }

        location / {
            try_files $uri $uri.html $uri/ =404;
        }
    }
}
